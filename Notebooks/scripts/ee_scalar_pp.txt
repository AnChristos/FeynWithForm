       + pow(alpha,2) * ( 64*pow(t,-2)*pow(pi,2)*pow(Mproton,4) - 64*pow(t,-1)
         *pow(pi,2)*pow(Mproton,2) + 16*pow(pi,2) );

      _ +=  + s*pow(alpha,2) * (  - 128*pow(t,-2)*pow(pi,2)*pow(Mproton,2) + 
         64*pow(t,-1)*pow(pi,2) );

      _ +=  + pow(s,2)*pow(alpha,2) * ( 64*pow(t,-2)*pow(pi,2) );
;
